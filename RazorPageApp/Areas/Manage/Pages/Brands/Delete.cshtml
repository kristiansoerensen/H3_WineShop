@page
@model RazorPageApp.Areas.Manage.Pages.Brands.DeleteModel
@{
    ViewData["Title"] = Model.Brand.Name;
    ViewData["ActivePage"] = ManageNavPages.BrandIndex;
}

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Brand</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Brand.Name)
        </dt>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Brand.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>